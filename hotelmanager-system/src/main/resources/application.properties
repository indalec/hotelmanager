spring.application.name=hotelmanager

#creates the table from scracth every time. Change to "update" to keep changes
spring.jpa.hibernate.ddl-auto=create

spring.datasource.url=jdbc:postgresql://localhost:5432/hotel_manager
spring.datasource.username=postgres
spring.datasource.password=admin


#added just in case, for compatibility
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
